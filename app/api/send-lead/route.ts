// This route is intentionally empty for static export compatibility
// Form submissions now send directly to Telegram from the client
export {};

/**
 * @deprecated This API route is no longer used.
 * Form submissions now use client-side Telegram API calls via lib/api.ts
 * to support static export on Cloudflare Pages.
 */